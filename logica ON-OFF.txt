Specifica per logica ON/OFF del bottone
1. Stato del bottone

Il bottone rappresenta due stati distinti: ON e OFF.

Lo stato iniziale all’apertura della pagina è spento (OFF).

2. Comportamento al click

Se il bottone viene cliccato quando è spento, esegue la funzione di accensione (ON).

Se il bottone viene cliccato quando è acceso, esegue la funzione di spegnimento (OFF).

I click successivi durante l’animazione vengono ignorati, ma il click successivo dopo che l’animazione è finita esegue la funzione contraria.

3. Elementi coinvolti

Gli elementi che cambiano visibilità con il bottone sono:

immagini

testo

logo

freccia

Gli elementi sono indipendenti e non fanno parte di un contenitore unico.

La freccia entra per ultima nella sequenza di accensione.

4. Sequenza di apparizione / sparizione

Tutti gli elementi appaiono insieme quando si passa a ON.

Tutti gli elementi scompaiono insieme quando si passa a OFF.

La transizione è sincrona, la stessa durata per tutti gli elementi.

Prima di procedere con l’animazione, si cambia lo stato (ON o OFF), poi parte la transizione.

5. Tipo di animazione

Gli elementi compariranno e scompariranno con animazioni di tipo fade o combinata (fade + spostamento/scale).

Il layout della pagina deve rimanere stabile durante l’animazione.

6. Timing e gestione

La logica delle animazioni deve essere controllata dal CSS tramite classi.

JS si occuperà solo di toggle dello stato e aggiunta/rimozione delle classi.

Non è necessario un evento di fine animazione per trigger aggiuntivi.

7. Contesto e manutenzione

Il contenuto e le animazioni devono caricarsi solo dopo che JS è attivo.

La struttura deve essere chiara e leggibile, in modo che la logica possa essere spiegata rapidamente e compresa facilmente in futuro.

La stessa logica ON/OFF dovrà poter essere riutilizzata in altre situazioni senza riscrivere il codice.

💡 Sintesi operativa:

Bottone cliccato → toggle stato ON/OFF

Aggiungi/rimuovi classi CSS agli elementi per animare comparsa/scomparsa

Tutto sincronizzato, stessa durata, freccia entra per ultima

Click ignorati durante animazione, layout stabile, JS solo per stato